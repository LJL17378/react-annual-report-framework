# React Annual Report Framework

一个现代化的年度报告展示框架，使用React、TypeScript和Vite构建，提供流畅的动画效果和响应式设计。

## 🚀 技术栈

### 核心框架
- **React 18** 
- **TypeScript**
- **Vite**

### UI & 样式
- **SCSS Modules**
- **Framer Motion**
- **Lucide React**

### 其他
- **Swiper** 

## 📋 环境要求

- **Node.js**: >= 20.0.0
- **pnpm**: >= 8.0.0 (推荐使用pnpm作为包管理器)

## 🚦 快速开始

### 1. 环境检查
确保你安装了正确版本的Node.js：
```bash
node --version  # 应该显示 v20.x.x 或更高
```

如果你使用nvm，可以运行：
```bash
nvm use
```

### 2. 安装依赖
```bash
pnpm install
```

### 3. 启动开发服务器
```bash
pnpm dev
```
开发服务器将在 http://localhost:5173 启动

### 4. 构建生产版本
```bash
pnpm build
```

### 5. 预览生产构建
```bash
pnpm preview
```

### 6. 代码检查
```bash
pnpm lint        # ESLint检查
pnpm typecheck   # TypeScript类型检查
```

## 📁 项目结构

```
src/
├── components/          # 可复用组件
│   ├── Layout/         # 布局组件
│   └── MusicPlayer/    # 音乐播放器组件
├── context/            # React Context
│   └── AppContext.tsx  # 应用全局状态
├── pages/              # 页面组件
│   ├── Cover.tsx       # 封面页
│   ├── Birthday.tsx    # 生日页
│   ├── Summary.tsx     # 总结页
│   └── *.module.scss   # 页面样式
├── App.tsx             # 应用根组件
├── main.tsx            # 应用入口
└── index.css           # 全局样式
```

## 🎨 设计特点

### 动画系统
- 使用Framer Motion实现流畅的页面过渡
- 基于Variants的动画状态管理
- 支持交互动画和滚动触发

### 样式架构
- SCSS Modules确保样式隔离
- 响应式设计支持
- 现代化的CSS特性

### 性能优化
- Vite的快速热更新
- 代码分割和懒加载
- 优化的构建输出

## 🔧 开发指南

### 添加新页面
1. 在 `src/pages/` 目录下创建新组件
2. 创建对应的 `.module.scss` 样式文件
3. 在 `App.tsx` 的Swiper组件中添加新页面

### 动画配置
使用Framer Motion的Variants系统：
```typescript
import { motion, Variants } from 'framer-motion';

const variants: Variants = {
  hidden: { opacity: 0, y: 20 },
  show: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.6, ease: "easeInOut" }
  }
};
```

### 样式编写
使用SCSS Modules确保样式隔离：
```scss
// Component.module.scss
.container {
  display: flex;
  flex-direction: column;

  .title {
    font-size: 2rem;
    color: #333;
  }
}
```

## 📦 构建与部署

### 构建配置
- 生产构建自动优化和压缩
- 支持环境变量配置
- 自动生成sourcemap

### 部署建议
- 静态文件托管（Vercel、Netlify、GitHub Pages）
- 配置适当的缓存策略
- 启用CDN加速

## 🐛 常见问题

### Node版本问题
如果遇到Node版本错误：
1. 使用nvm安装正确版本：`nvm install 20`
2. 或者更新现有Node版本

### 依赖安装问题
如果pnpm安装失败：
```bash
# 清除缓存并重试
pnpm store prune
pnpm install
```

### 类型检查错误
运行 `pnpm typecheck` 查看详细错误信息，确保所有TypeScript类型正确。

## 📄 许可证

MIT License

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建Pull Request

## 📞 支持

如有问题，请提交Issue或联系维护者。
